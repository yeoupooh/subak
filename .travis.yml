install:
 - pushd server/node/server
 - npm install
 - popd

before_script:
 - pushd server/node/server
 - npm start &
 - popd
 - pushd client/groovy/src/main/resources
 - cp subak.config.sample.json subak.config.json
 - popd

script:
 - pushd client/groovy
 - ./gradlew jar test
 - popd
